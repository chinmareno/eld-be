{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd project && python manage.py migrate --noinput",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/wsgi.py"
    }
  ]
}
