{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd project && python manage.py migrate --noinput && python manage.py bootstrap_superuser",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/wsgi.py"
    }
  ]
}
